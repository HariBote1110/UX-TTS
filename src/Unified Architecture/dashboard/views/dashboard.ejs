<!DOCTYPE html>
<html lang="ja" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>サーバー選択 - UX TTS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body { background-color: #2b2d31; color: white; min-height: 100vh; }
        .container { padding-top: 40px; padding-bottom: 40px; }
        .server-card { 
            background-color: #313338; border: none; 
            transition: transform 0.2s, background-color 0.2s; 
            text-decoration: none; color: white; border-radius: 12px;
        }
        .server-card:hover { 
            transform: translateY(-5px); background-color: #404249; color: white; 
        }
        .server-icon { 
            width: 80px; height: 80px; object-fit: cover; 
            background-color: #1e1f22; 
            display: flex; align-items: center; justify-content: center; 
            font-size: 1.5rem; margin: 0 auto 15px; box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }
        @media (max-width: 576px) {
            .container { padding-top: 20px; }
            .server-icon { width: 64px; height: 64px; font-size: 1.2rem; }
            .card-title { font-size: 1rem; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-5 border-bottom border-secondary pb-3">
            <h5 class="m-0 text-truncate"><i class="bi bi-person-circle me-2"></i><%= user.username %></h5>
            <a href="/logout" class="btn btn-sm btn-outline-danger"><i class="bi bi-box-arrow-right me-1"></i>ログアウト</a>
        </div>
        
        <% if (announcement && announcement.enabled && announcement.dashboard) { %>
            <div class="alert alert-<%= announcement.dashboard.type %> alert-dismissible fade show mb-4 shadow-sm" role="alert">
                <h5 class="alert-heading"><i class="bi bi-megaphone-fill me-2"></i><%= announcement.dashboard.title %></h5>
                <p class="mb-0" style="white-space: pre-line;"><%= announcement.dashboard.message %></p>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>

        <h4 class="mb-4 text-light text-center text-md-start"><i class="bi bi-grid-fill me-2"></i>サーバーを選択</h4>
        
        <div class="row g-3 g-md-4">
            <% guilds.forEach(guild => { %>
                <div class="col-6 col-md-4 col-lg-3">
                    <a href="/dashboard/<%= guild.id %>" class="card server-card h-100 py-4 shadow-sm">
                        <div class="card-body text-center p-2">
                            <% if (guild.icon) { %>
                                <img src="<%= guild.icon %>" class="rounded-circle server-icon">
                            <% } else { %>
                                <div class="rounded-circle server-icon text-white"><%= guild.name.substring(0,1) %></div>
                            <% } %>
                            <h6 class="card-title text-truncate px-1 mb-0"><%= guild.name %></h6>
                        </div>
                    </a>
                </div>
            <% }); %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>